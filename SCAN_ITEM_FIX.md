# Исправление: Объединение одинаковых товаров при сканировании

## Проблема

При сканировании одного и того же товара несколько раз, создавались **отдельные позиции** в продаже вместо увеличения количества существующей позиции.

### До исправления:

```
Сканирование товара ID 19 (qty 1)  → Позиция 1: товар 19, qty 1
Сканирование товара ID 19 (qty 1)  → Позиция 2: товар 19, qty 1
Сканирование товара ID 19 (qty 1)  → Позиция 3: товар 19, qty 1

Итого: 3 позиции с qty=1 каждая ❌
```

### После исправления:

```
Сканирование товара ID 19 (qty 1)  → Позиция 1: товар 19, qty 1
Сканирование товара ID 19 (qty 1)  → Позиция 1: товар 19, qty 2 ✅
Сканирование товара ID 19 (qty 1)  → Позиция 1: товар 19, qty 3 ✅

Итого: 1 позиция с qty=3 ✅
```

## Решение

### Изменённый файл:
[sales/views.py:379-399](sales/views.py#L379-L399)

### Что было изменено:

**До:**
```python
# Добавляем позицию
sale_item = SaleItem.objects.create(
    sale=sale,
    product=product,
    batch=batch,
    quantity=quantity,
    unit_price=unit_price
)
```

**После:**
```python
# Проверяем, есть ли уже такой товар в продаже
existing_item = SaleItem.objects.filter(
    sale=sale,
    product=product,
    batch=batch
).first()

if existing_item:
    # Увеличиваем количество существующей позиции
    existing_item.quantity += quantity
    existing_item.save()
    sale_item = existing_item
else:
    # Создаём новую позицию
    sale_item = SaleItem.objects.create(
        sale=sale,
        product=product,
        batch=batch,
        quantity=quantity,
        unit_price=unit_price
    )
```

## Логика работы

1. **Проверка существующей позиции:**
   - Ищем `SaleItem` с тем же `product` и `batch` в текущей продаже

2. **Если позиция найдена:**
   - Увеличиваем `quantity` на отсканированное количество
   - Сохраняем изменения
   - `line_total` пересчитывается автоматически при save()

3. **Если позиция не найдена:**
   - Создаём новую позицию как раньше

## Тестирование

### Тест 1: Сканирование одного товара несколько раз

```bash
# Scan 1
POST /api/sales/sales/scan_item/
Body: {"session": 1, "product": 19, "quantity": 1}
Результат: 1 позиция, qty=1

# Scan 2
POST /api/sales/sales/scan_item/
Body: {"session": 1, "product": 19, "quantity": 1}
Результат: 1 позиция, qty=2 ✅

# Scan 3
POST /api/sales/sales/scan_item/
Body: {"session": 1, "product": 19, "quantity": 2}
Результат: 1 позиция, qty=4 ✅
```

### Тест 2: Сканирование разных товаров

```bash
# Scan товар 19
POST /api/sales/sales/scan_item/
Body: {"session": 1, "product": 19, "quantity": 1}
Результат: 1 позиция (товар 19)

# Scan товар 18
POST /api/sales/sales/scan_item/
Body: {"session": 1, "product": 18, "quantity": 1}
Результат: 2 позиции (товар 19 и 18) ✅

# Scan товар 19 снова
POST /api/sales/sales/scan_item/
Body: {"session": 1, "product": 19, "quantity": 1}
Результат: 2 позиции (товар 19 qty=2, товар 18 qty=1) ✅
```

### Реальный тест:

**Результаты:**
```
Сканирование 1: product 19, qty 1
  ✅ Items: 1, product 19 qty=1, total=300000

Сканирование 2: product 19, qty 1
  ✅ Items: 1, product 19 qty=2, total=600000

Сканирование 3: product 19, qty 2
  ✅ Items: 1, product 19 qty=4, total=1200000
```

## Frontend поведение

### Что изменилось для frontend:

**Ничего!** API остаётся полностью совместимым.

Ответ всегда возвращает полную продажу со всеми позициями:

```typescript
const sale = await scanItem({
  session: 1,
  product: 19,
  quantity: 1
});

// sale.items всегда содержит актуальный список позиций
console.log(sale.items);
// [
//   { product: 19, quantity: 2, line_total: 600000 }
// ]
```

### Рекомендации для UI:

1. **Показывать количество в реальном времени:**
```typescript
const handleScan = async (productId: number) => {
  const sale = await scanItem({
    session: currentSession.id,
    product: productId,
    quantity: 1
  });

  // Обновляем список товаров
  setItems(sale.items);

  // Показываем уведомление
  const item = sale.items.find(i => i.product === productId);
  toast.success(`${item.product_name}: ${item.quantity} шт.`);
};
```

2. **Подсветка изменённой позиции:**
```typescript
// После сканирования найти какая позиция обновилась
const updatedItem = newSale.items.find(item =>
  item.product === scannedProductId
);

// Подсветить в UI на 1 секунду
highlightItem(updatedItem.id);
```

## Обработка партий (batch)

Если товары имеют разные партии, они будут **разными позициями**:

```
Товар 19, партия A, qty 1  → Позиция 1
Товар 19, партия B, qty 1  → Позиция 2 (другая партия!)
Товар 19, партия A, qty 1  → Позиция 1: qty=2 ✅
```

Логика фильтра:
```python
existing_item = SaleItem.objects.filter(
    sale=sale,
    product=product,
    batch=batch  # ← Учитывается партия!
).first()
```

## Преимущества

### ✅ Для пользователя:
- Чистый чек без дублей
- Легко увидеть общее количество товара
- Быстрее проверить чек

### ✅ Для системы:
- Меньше записей в базе данных
- Проще отчётность
- Правильная работа резервирования товара

### ✅ Для кассира:
- Естественное поведение как в обычной кассе
- Не нужно вручную объединять позиции
- Меньше ошибок при подсчёте

## Edge Cases

### 1. Товар без партии (batch=None)
```python
# Scan 1
{"product": 19, "quantity": 1}  # batch не указан
# Scan 2
{"product": 19, "quantity": 1}  # batch не указан
# ✅ Объединятся в одну позицию
```

### 2. Товар с указанной партией
```python
# Scan 1
{"product": 19, "batch": 5, "quantity": 1}
# Scan 2
{"product": 19, "batch": 5, "quantity": 1}
# ✅ Объединятся (та же партия)

# Scan 3
{"product": 19, "batch": 6, "quantity": 1}
# ✅ Новая позиция (другая партия)
```

### 3. Разная цена для одного товара
Цена берётся из `product.pricing.sale_price` при создании **первой** позиции.

При последующих сканированиях того же товара цена **не обновляется**.

Если нужна разная цена → используйте `POST /api/sales/sales/` напрямую.

## Миграция существующих данных

**Не требуется!**

Изменение затрагивает только новые сканирования. Существующие продажи с дублирующимися позициями остаются без изменений.

## Откат изменений

Если нужно вернуть старое поведение (создавать новые позиции вместо объединения):

```python
# Убрать проверку на existing_item
# Оставить только:
sale_item = SaleItem.objects.create(
    sale=sale,
    product=product,
    batch=batch,
    quantity=quantity,
    unit_price=unit_price
)
```

## Резюме

### Что было:
- ❌ Каждое сканирование = новая позиция
- ❌ Дубли в чеке
- ❌ Запутанный итоговый чек

### Что стало:
- ✅ Сканирование того же товара увеличивает количество
- ✅ Чистый чек без дублей
- ✅ Естественное поведение POS системы

### Endpoint:
```
POST /api/sales/sales/scan_item/
```

### Поведение:
- Тот же товар + та же партия = **увеличение qty**
- Тот же товар + другая партия = **новая позиция**
- Другой товар = **новая позиция**

Готово! 🎉
