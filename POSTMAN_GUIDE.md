# Как использовать Postman коллекцию

## Шаг 1: Импорт коллекции

1. Открой Postman
2. Нажми **Import** в левом верхнем углу
3. Выбери файл `ERP_v2_Full.postman_collection.json`
4. Коллекция появится в списке слева

## Шаг 2: Настройка переменных

Коллекция уже настроена с базовыми переменными:

- `base_url` = `http://localhost:8000`
- `access_token` = (автоматически заполняется после логина)
- `refresh_token` = (автоматически заполняется после логина)

Если нужно изменить URL, перейди в настройки коллекции:
1. Нажми на коллекцию правой кнопкой
2. **Edit**
3. Вкладка **Variables**
4. Измени `base_url` на нужный

## Шаг 3: Первый запуск

### 3.1 Регистрация

1. Открой папку **Authentication**
2. Выбери запрос **Register**
3. Измени данные в Body:
   ```json
   {
     "username": "твой_логин",
     "email": "твой_email@example.com",
     "password": "твой_пароль",
     "password2": "твой_пароль",
     "first_name": "Имя",
     "last_name": "Фамилия",
     "store_name": "Название магазина",
     "phone": "+998901234567"
   }
   ```
4. Нажми **Send**

### 3.2 Вход (Login)

1. Выбери запрос **Login**
2. Укажи свои данные:
   ```json
   {
     "username": "твой_логин",
     "password": "твой_пароль"
   }
   ```
3. Нажми **Send**
4. **Токен автоматически сохранится** в переменных коллекции

### 3.3 Получить список магазинов ⭐ ВАЖНО!

1. Выбери запрос **Get My Stores**
2. Нажми **Send**
3. **tenant_key автоматически сохранится** из первого магазина
4. Теперь все последующие запросы будут использовать этот магазин

**Важно:** Без tenant_key большинство запросов не будут работать!

### 3.4 Проверка профиля

1. Выбери запрос **Get Profile**
2. Нажми **Send**
3. Если всё ок, ты увидишь свои данные

## Шаг 4: Создание сотрудника (опционально)

Если хочешь добавить нового кассира:

1. **Authentication** → **Get My Stores** (если ещё не сделал)
2. Перейди в **Products** → **Create Product** (чтобы убедиться что tenant_key работает)
3. Если получаешь ошибку "Не указан магазин" - проверь что tenant_key сохранился в Variables

## Шаг 5: Работа с кассой (типичный флоу)

### 5.1 Создай товар

1. Перейди в **Products** → **Create Product**
2. Нажми **Send** (или измени данные)
3. Запомни `id` товара из ответа

### 5.2 Открой смену

1. **Sales (Cashier)** → **Open Session**
2. Укажи начальный баланс:
   ```json
   {
     "opening_balance": 100000
   }
   ```
3. Запомни `session_id` из ответа

### 4.3 Сканируй товары

1. **Scan Item (Add to Sale)**
2. Укажи session и product:
   ```json
   {
     "session": 1,
     "product": 18,
     "quantity": 2,
     "batch": null
   }
   ```
3. Можно сканировать несколько раз
4. Каждый раз будет возвращаться обновлённая продажа

### 4.4 Проверь текущую продажу

1. **Get Current Sale**
2. В URL измени параметр `session=1` на твой session_id
3. Увидишь все товары и сумму

### 4.5 Завершить продажу

1. **Checkout (Complete Sale)**
2. В URL измени `:sale_id` на id твоей продажи
3. Укажи способ оплаты:
   ```json
   {
     "payments": [
       {
         "payment_method": "cash",
         "amount": 150000,
         "received_amount": 200000
       }
     ]
   }
   ```
4. Продажа завершится, товар спишется со склада

### 4.6 Закрой смену

1. **Close Session**
2. В URL измени `:session_id`
3. Укажи фактическую сумму:
   ```json
   {
     "actual_cash": 250000
   }
   ```

## Шаг 5: Аналитика

### 5.1 Отчёт за сегодня

**Analytics** → **Today Report**

### 5.2 Топ товары за 30 дней

**Analytics** → **Top Products**

Параметры в URL:
- `limit=10` - количество товаров
- `order_by=revenue` - сортировка (revenue, quantity, profit)

### 5.3 Алерты по складу

**Analytics** → **Low Stock Alerts** - товары с низким остатком

**Analytics** → **Out of Stock** - товары с нулевым остатком

## Шаг 6: Задачи (Tasks)

### 6.1 Создать задачу

1. **Tasks** → **Create Task**
2. Укажи детали:
   ```json
   {
     "title": "Проверить остатки",
     "description": "Инвентаризация склада",
     "priority": "high",
     "category": "inventory",
     "assigned_to": 1,
     "due_date": "2025-01-20T18:00:00Z"
   }
   ```

### 6.2 Мои задачи

**Tasks** → **My Tasks**

### 6.3 Статистика

**Tasks** → **Task Stats**

## Полезные советы

### Автоматическая авторизация

Токен уже настроен на уровне коллекции. После логина все запросы автоматически используют токен из переменной `{{access_token}}`.

### Изменение параметров в URL

В запросах с параметрами (`:sale_id`, `:session_id`) не забывай их менять:

1. Нажми на **Params** tab
2. Измени значение **Path Variables**

Или просто измени в URL:
```
{{base_url}}/api/sales/sales/123/checkout/
                                ^^^
                              твой ID
```

### Тестирование разных сценариев

1. **Успешная продажа:**
   - Open Session → Scan Item → Checkout → Close Session

2. **Отмена товара:**
   - Scan Item → Remove Item → Scan другой товар → Checkout

3. **Несколько способов оплаты:**
   - В Checkout укажи несколько payments (наличные + карта)

4. **Проверка остатков:**
   - Сделай несколько продаж
   - Проверь Low Stock Alerts
   - Создай задачу на пополнение

## Переменные в коллекции

Все переменные находятся на уровне коллекции:

| Переменная | Описание | Откуда берётся |
|------------|----------|----------------|
| `base_url` | URL сервера | Ручная настройка |
| `access_token` | JWT токен | Автоматически из Login |
| `refresh_token` | Refresh токен | Автоматически из Login |

## Troubleshooting

### Ошибка 401 (Unauthorized)

- Проверь что токен сохранён: Variables → `access_token`
- Если токен истёк, сделай Login снова
- Токен живёт 60 минут

### Ошибка 400 (Bad Request)

- Проверь формат данных в Body
- Убедись что все обязательные поля заполнены
- Смотри текст ошибки в Response

### Ошибка 404 (Not Found)

- Проверь что URL правильный
- Проверь что ID существует (например, product_id)
- Убедись что сервер запущен

### Сервер не отвечает

```bash
cd /Users/akkanat/Projects/erp_v2/new_backend
source venv/bin/activate
python manage.py runserver
```

## Структура коллекции

```
ERP v2 - Full API Collection
├── Authentication
│   ├── Register
│   ├── Login (сохраняет токен автоматически)
│   └── Get Profile
├── Products
│   ├── List Products
│   ├── Create Product
│   ├── Get Product by ID
│   └── Low Stock Products
├── Sales (Cashier) ⭐ Новые эндпоинты для кассы
│   ├── Open Session
│   ├── Get Current Session
│   ├── Scan Item (Add to Sale) ⭐
│   ├── Get Current Sale ⭐
│   ├── Add Item to Sale ⭐
│   ├── Remove Item from Sale ⭐
│   ├── Checkout (Complete Sale) ⭐
│   ├── Close Session
│   ├── List Sales
│   └── Today Sales
├── Analytics ⭐ Новая секция
│   ├── Daily Sales Reports
│   ├── Today Report
│   ├── Period Report
│   ├── Sales Trends
│   ├── Product Performance
│   ├── Top Products
│   ├── Slow Movers
│   ├── Customer Analytics
│   ├── Customer Segments
│   ├── At Risk Customers
│   ├── Inventory Snapshots
│   ├── Latest Inventory
│   ├── Low Stock Alerts
│   └── Out of Stock
├── Tasks ⭐ Новая секция
│   ├── List Tasks
│   ├── Create Task
│   ├── Get Task by ID
│   ├── My Tasks
│   ├── Today Tasks
│   ├── Overdue Tasks
│   ├── Task Stats
│   ├── Start Task
│   ├── Complete Task
│   ├── Add Comment
│   └── List Task Templates
└── Customers
    ├── List Customers
    └── Create Customer
```

⭐ = Новые эндпоинты в этой версии

## Следующие шаги

1. Попробуй все запросы по порядку
2. Создай несколько товаров
3. Открой смену и сделай несколько продаж
4. Проверь аналитику
5. Создай пару задач
6. Посмотри на Swagger UI для интерактивной документации: http://localhost:8000/swagger/

Удачи! 🚀
