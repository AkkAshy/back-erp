# Products Module - Модуль Управления Товарами

## Обзор изменений

Модуль продуктов был полностью реорганизован для улучшения структуры базы данных и добавления функциональности учета партий товаров.

### Основные изменения:

1. **Нормализация модели Product** - разделение на три связанных модели
2. **Добавление системы учета партий** - поддержка FIFO/FEFO стратегий
3. **Переход на партийный учет остатков** - quantity теперь вычисляется из партий

---

## 📊 Структура моделей

### 1. Product (Основная модель товара)

**Файл:** `products/models.py:292-524`

**Описание:** Основная информация о товаре без цен и остатков.

**Поля:**
- `name` - Название товара
- `slug` - URL-friendly название
- `description` - Описание
- `category` - Категория (FK → Category)
- `sku` - Артикул (уникальный)
- `barcode` - Штрих-код (опционально)
- `unit` - Единица измерения (FK → Unit)
- `main_image` - Главное изображение
- `weight` - Вес в кг
- `volume` - Объем в литрах
- `is_active` - Активен ли товар
- `is_featured` - Рекомендуемый товар
- `created_at`, `updated_at` - Метаданные

**Вычисляемые свойства:**
- `margin` - Наценка в % (из ProductPricing)
- `profit` - Прибыль с единицы (из ProductPricing)
- `is_low_stock` - Достиг ли минимального остатка (из ProductInventory)
- `stock_status` - Статус остатка: `unlimited`, `in_stock`, `low_stock`, `out_of_stock`

---

### 2. ProductPricing (Цены товара)

**Файл:** `products/models.py:526-601`

**Описание:** Ценовая информация товара (связь 1:1 с Product).

**Поля:**
- `product` - Товар (OneToOneField → Product)
- `cost_price` - Себестоимость (цена закупки)
- `sale_price` - Розничная цена
- `wholesale_price` - Оптовая цена (опционально)
- `tax_rate` - Ставка НДС в % (например: 12, 15, 20)
- `updated_at` - Дата обновления

**Вычисляемые свойства:**
- `margin` - Наценка: `((sale_price - cost_price) / cost_price) * 100`
- `profit` - Прибыль: `sale_price - cost_price`

**Валидация:**
- Цена продажи не может быть меньше себестоимости

---

### 3. ProductInventory (Настройки учета)

**Файл:** `products/models.py:603-692`

**Описание:** Настройки учета остатков товара (связь 1:1 с Product).

**⚠️ ВАЖНО:** Поле `quantity` было удалено! Теперь это вычисляемое свойство.

**Поля:**
- `product` - Товар (OneToOneField → Product)
- `min_quantity` - Минимальный остаток (для уведомлений)
- `max_quantity` - Максимальный остаток (опционально)
- `track_inventory` - Вести ли учет остатков
- `updated_at` - Дата обновления

**Вычисляемые свойства:**
- `quantity` - **Сумма всех активных партий товара** (ProductBatch.quantity)
- `is_low_stock` - Проверка: `quantity <= min_quantity`
- `stock_status` - Статус: `unlimited` / `out_of_stock` / `low_stock` / `in_stock`

**Пример:**
```python
# Раньше (удалено):
product.inventory.quantity = 100  # ❌ Больше не работает!

# Теперь:
quantity = product.inventory.quantity  # ✅ Read-only, вычисляется из партий
# Для изменения количества нужно создать/обновить партии
```

---

### 4. ProductBatch (Партия товара) 🆕

**Файл:** `products/models.py:694-821`

**Описание:** Партия товара с индивидуальной ценой закупки, сроком годности и количеством.

**Поля:**
- `product` - Товар (FK → Product, related_name='batches')
- `batch_number` - Номер партии/накладной
- `quantity` - Количество в партии
- `purchase_price` - Цена закупки этой партии
- `manufacturing_date` - Дата производства (опционально)
- `expiry_date` - Срок годности (опционально)
- `supplier_name` - Название поставщика (временно, строка)
- `notes` - Примечания
- `is_active` - Активна ли партия
- `received_at` - Дата поступления (auto_now_add)
- `updated_at` - Дата обновления (auto_now)

**Вычисляемые свойства:**
- `is_expired` - Истёк ли срок годности
- `days_until_expiry` - Дней до истечения срока
- `is_near_expiry(days=30)` - Истекает ли в ближайшие N дней

**Meta:**
- Сортировка по умолчанию: `['expiry_date', 'received_at']` - **FEFO (First Expired, First Out)**
- Индексы на: `[product, is_active]`, `expiry_date`, `received_at`

**Валидация:**
- Срок годности должен быть позже даты производства

**Пример использования:**
```python
# Создание партии
batch = ProductBatch.objects.create(
    product=product,
    batch_number="INV-2024-001",
    quantity=50,
    purchase_price=100.00,
    expiry_date=date(2025, 12, 31),
    supplier_name="ООО Поставщик"
)

# Получение общего количества товара
total_qty = product.inventory.quantity  # Сумма всех активных партий

# Получение партий с истекающим сроком
expiring_batches = product.batches.filter(
    expiry_date__lte=date.today() + timedelta(days=30),
    is_active=True
)
```

---

## 🔄 API Endpoints

### Products (Товары)

```
GET    /api/products/products/              - Список товаров
POST   /api/products/products/              - Создать товар
GET    /api/products/products/{id}/         - Детали товара
PUT    /api/products/products/{id}/         - Обновить товар
PATCH  /api/products/products/{id}/         - Частичное обновление
DELETE /api/products/products/{id}/         - Удалить товар

# Дополнительные действия
GET    /api/products/products/low_stock/    - Товары с низким остатком
GET    /api/products/products/out_of_stock/ - Товары без остатков
GET    /api/products/products/featured/     - Рекомендуемые товары
POST   /api/products/products/{id}/add_attribute/ - Добавить атрибут
POST   /api/products/products/{id}/add_image/     - Добавить изображение
PATCH  /api/products/products/{id}/update_quantity/  - ⚠️ Устаревший (используйте партии)
PATCH  /api/products/products/{id}/adjust_quantity/ - ⚠️ Устаревший (используйте партии)
```

**Query параметры для списка товаров:**
- `category` - Фильтр по категории
- `unit` - Фильтр по единице измерения
- `is_active` - Активные/неактивные
- `is_featured` - Рекомендуемые
- `stock_status` - `in_stock`, `out_of_stock`, `low_stock`, `unlimited`
- `min_price`, `max_price` - Диапазон цен
- `has_images` - Наличие изображений
- `search` - Поиск по name, description, sku, barcode
- `ordering` - Сортировка: `name`, `created_at`, `updated_at`, `-created_at`

---

### Product Batches (Партии товаров) 🆕

```
GET    /api/products/batches/              - Список партий
POST   /api/products/batches/              - Создать партию
GET    /api/products/batches/{id}/         - Детали партии
PUT    /api/products/batches/{id}/         - Обновить партию
PATCH  /api/products/batches/{id}/         - Частичное обновление
DELETE /api/products/batches/{id}/         - Удалить партию

# Специальные endpoints
GET    /api/products/batches/expired/      - Истёкшие партии
GET    /api/products/batches/near_expiry/  - Партии с истекающим сроком
```

**Query параметры:**
- `product` - Фильтр по товару (ID)
- `is_active` - Активные/неактивные партии
- `expired=true` - Только истёкшие партии
- `expired=false` - Только действительные партии
- `near_expiry=true&days=30` - Партии, истекающие в ближайшие N дней
- `search` - Поиск по batch_number, supplier_name
- `ordering` - Сортировка: `received_at`, `expiry_date`, `quantity`

**Примеры запросов:**

```bash
# Получить все партии товара
curl http://localhost:8000/api/products/batches/?product=5

# Получить истёкшие партии
curl http://localhost:8000/api/products/batches/expired/

# Получить партии, истекающие в ближайшие 7 дней
curl http://localhost:8000/api/products/batches/near_expiry/?days=7

# Создать партию
curl -X POST http://localhost:8000/api/products/batches/ \
  -H "Content-Type: application/json" \
  -d '{
    "product": 5,
    "batch_number": "INV-2024-001",
    "quantity": 100,
    "purchase_price": 50.00,
    "expiry_date": "2025-12-31",
    "supplier_name": "ООО Поставщик"
  }'
```

---

## 📝 Serializers

### ProductCreateUpdateSerializer

**Файл:** `products/serializers.py:156-243`

**Используется для:** Создания и обновления товаров

**Поля:**
```python
# Основные поля Product
- name, slug, description
- category, sku, barcode, unit
- main_image, weight, volume
- is_active, is_featured

# Поля для ProductPricing (write_only)
- cost_price
- sale_price
- wholesale_price (опционально)
- tax_rate (по умолчанию 0)

# Поля для ProductInventory (write_only)
- min_quantity (по умолчанию 0)
- max_quantity (опционально)
- track_inventory (по умолчанию True)

# ⚠️ ВАЖНО: quantity удалено!
```

**Пример создания товара:**
```json
{
  "name": "Молоко 3.2%",
  "slug": "milk-3-2",
  "description": "Молоко пастеризованное",
  "category": 1,
  "sku": "MILK-001",
  "barcode": "4607014234567",
  "unit": 2,
  "is_active": true,
  "is_featured": false,

  // Цены
  "cost_price": 45.00,
  "sale_price": 65.00,
  "wholesale_price": 55.00,
  "tax_rate": 10,

  // Настройки учета (без quantity!)
  "min_quantity": 10,
  "max_quantity": 1000,
  "track_inventory": true
}
```

После создания товара нужно создать партию для добавления остатков!

---

### ProductDetailSerializer

**Файл:** `products/serializers.py:204-237`

**Используется для:** Детального просмотра товара

**Включает:**
- Полную информацию о товаре
- `pricing` - вложенный ProductPricingSerializer
- `inventory` - вложенный ProductInventorySerializer (с вычисляемым quantity)
- `batches` - массив ProductBatchSerializer (все партии товара)
- `attributes` - атрибуты товара
- `images` - изображения товара

---

### ProductBatchSerializer

**Файл:** `products/serializers.py:121-151`

**Поля:**
```json
{
  "id": 1,
  "product": 5,
  "product_name": "Молоко 3.2%",  // read_only
  "batch_number": "INV-2024-001",
  "quantity": 50,
  "purchase_price": 45.00,
  "manufacturing_date": "2024-10-01",
  "expiry_date": "2024-12-31",
  "supplier_name": "ООО Поставщик",
  "notes": "Первая партия",
  "is_active": true,

  // Вычисляемые поля (read_only)
  "is_expired": false,
  "days_until_expiry": 45,
  "is_near_expiry": true,

  "received_at": "2024-10-01T10:00:00Z",
  "updated_at": "2024-10-01T10:00:00Z"
}
```

---

## 🔍 ViewSets

### ProductBatchViewSet

**Файл:** `products/views.py:130-212`

**Особенности:**
- Сортировка по умолчанию: FEFO (expiry_date, received_at)
- Фильтры: product, is_active, expired, near_expiry
- Поиск: batch_number, supplier_name

**Custom Actions:**

1. **expired** - Получить истёкшие партии
   - URL: `/api/products/batches/expired/`
   - Фильтр: `expiry_date < today AND is_active=True`

2. **near_expiry** - Партии с истекающим сроком
   - URL: `/api/products/batches/near_expiry/?days=30`
   - Фильтр: `today <= expiry_date <= today+30 AND is_active=True`

---

## 🗄️ Миграции

### Выполненные миграции:

1. **0001_initial** - Начальная структура с нормализованными моделями
   - Создание Product, ProductPricing, ProductInventory
   - Все таблицы созданы

2. **0002_productbatch_alter_productinventory_options_and_more**
   - Создание ProductBatch
   - Удаление поля quantity из ProductInventory
   - Удаление индекса на quantity
   - Добавление индексов для ProductBatch

**Таблицы в БД:**
```
products_unit
products_category
products_attribute
products_attribute_value
products_product
products_product_pricing         ← Новая (1:1)
products_product_inventory       ← Модифицирована (удалено quantity)
products_product_batch           ← Новая (1:N)
products_product_attribute
products_product_image
```

---

## 🚨 Breaking Changes (Критические изменения)

### 1. Удалено поле quantity из ProductInventory

**Раньше:**
```python
# ❌ Больше не работает!
product.inventory.quantity = 100
product.inventory.save()

# ❌ Не работает
ProductInventory.objects.create(product=product, quantity=50)
```

**Теперь:**
```python
# ✅ Quantity теперь read-only свойство
total = product.inventory.quantity  # Вычисляется из партий

# ✅ Для изменения остатков используйте партии
ProductBatch.objects.create(
    product=product,
    batch_number="INV-001",
    quantity=50,
    purchase_price=100.00
)
```

### 2. Изменены endpoints для обновления количества

**Устаревшие endpoints (всё ещё работают, но не рекомендуются):**
- `PATCH /api/products/products/{id}/update_quantity/`
- `PATCH /api/products/products/{id}/adjust_quantity/`

**Рекомендуется использовать:**
```bash
# Создание партии для добавления остатков
POST /api/products/batches/
{
  "product": 5,
  "batch_number": "INV-2024-001",
  "quantity": 100,
  "purchase_price": 45.00
}

# Обновление количества в партии
PATCH /api/products/batches/{batch_id}/
{
  "quantity": 80
}
```

### 3. ProductCreateUpdateSerializer не принимает quantity

**Раньше:**
```json
{
  "name": "Товар",
  "cost_price": 100,
  "sale_price": 150,
  "quantity": 50  // ❌ Удалено!
}
```

**Теперь:**
```json
{
  "name": "Товар",
  "cost_price": 100,
  "sale_price": 150
  // quantity убрано!
}
```

После создания товара создайте партию отдельным запросом.

---

## 🎯 Сценарии использования

### Сценарий 1: Создание нового товара с остатками

```python
# 1. Создать товар
product_data = {
    "name": "Молоко 3.2%",
    "sku": "MILK-001",
    "category": 1,
    "unit": 2,
    "cost_price": 45.00,
    "sale_price": 65.00,
    "min_quantity": 10,
    "track_inventory": True
}
response = requests.post('/api/products/products/', json=product_data)
product_id = response.json()['id']

# 2. Добавить первую партию
batch_data = {
    "product": product_id,
    "batch_number": "INV-2024-001",
    "quantity": 100,
    "purchase_price": 45.00,
    "expiry_date": "2024-12-31"
}
requests.post('/api/products/batches/', json=batch_data)

# 3. Проверить общее количество
response = requests.get(f'/api/products/products/{product_id}/')
print(response.json()['inventory']['quantity'])  # 100
```

### Сценарий 2: Поступление новой партии товара

```python
# Создать партию
batch_data = {
    "product": 5,
    "batch_number": "INV-2024-002",
    "quantity": 50,
    "purchase_price": 47.00,  # Новая цена закупки
    "manufacturing_date": "2024-11-01",
    "expiry_date": "2025-01-31",
    "supplier_name": "ООО Новый Поставщик"
}
requests.post('/api/products/batches/', json=batch_data)

# Теперь общее количество = 150 (100 + 50)
```

### Сценарий 3: Продажа товара (FEFO)

```python
# 1. Получить партии товара с сортировкой FEFO
response = requests.get(f'/api/products/batches/?product={product_id}&is_active=true')
batches = response.json()['results']

# 2. Первая партия - с самым ранним сроком годности (FEFO)
first_batch = batches[0]

# 3. Уменьшить количество в партии
new_quantity = first_batch['quantity'] - 10  # Продали 10 шт
requests.patch(
    f'/api/products/batches/{first_batch["id"]}/',
    json={"quantity": new_quantity}
)

# 4. Если партия опустошена, деактивировать
if new_quantity <= 0:
    requests.patch(
        f'/api/products/batches/{first_batch["id"]}/',
        json={"is_active": False}
    )
```

### Сценарий 4: Мониторинг сроков годности

```python
# Получить партии, истекающие в ближайшие 7 дней
response = requests.get('/api/products/batches/near_expiry/?days=7')
expiring_batches = response.json()['results']

for batch in expiring_batches:
    print(f"Партия {batch['batch_number']} истекает через {batch['days_until_expiry']} дней")
    print(f"Товар: {batch['product_name']}, Остаток: {batch['quantity']}")

# Получить истёкшие партии
response = requests.get('/api/products/batches/expired/')
expired_batches = response.json()['results']

for batch in expired_batches:
    # Деактивировать истёкшую партию
    requests.patch(
        f'/api/products/batches/{batch["id"]}/',
        json={"is_active": False}
    )
```

### Сценарий 5: Инвентаризация

```python
# 1. Получить текущее количество
response = requests.get(f'/api/products/products/{product_id}/')
current_qty = response.json()['inventory']['quantity']

# 2. Фактическое количество после инвентаризации
actual_qty = 95

# 3. Создать корректировочную партию
adjustment = actual_qty - current_qty  # -5 (недостача)

if adjustment != 0:
    requests.post('/api/products/batches/', json={
        "product": product_id,
        "batch_number": f"ADJ-{date.today()}",
        "quantity": abs(adjustment),
        "purchase_price": 0,  # Корректировка
        "notes": "Инвентаризация" if adjustment > 0 else "Недостача при инвентаризации"
    })
```

---

## 📚 Дополнительная информация

### Стратегии управления запасами

**FEFO (First Expired, First Out)** - используется по умолчанию
- Партии сортируются по сроку годности
- Сначала продаются товары с ближайшим сроком
- Идеально для продуктов питания и медикаментов

**FIFO (First In, First Out)** - можно использовать
- Партии сортируются по дате поступления
- Сначала продаются старые поступления
- Подходит для непортящихся товаров

Для переключения на FIFO измените сортировку:
```python
batches = ProductBatch.objects.filter(
    product=product,
    is_active=True
).order_by('received_at')  # FIFO вместо expiry_date
```

### Индексы базы данных

Для оптимизации запросов созданы следующие индексы:

**ProductInventory:**
- `track_inventory`

**ProductBatch:**
- `(product, is_active)` - для быстрого получения активных партий товара
- `expiry_date` - для поиска истекающих партий
- `received_at` - для FIFO сортировки

### Permissions

Все ViewSet'ы используют `IsTenantUser` permission:
- Требуется авторизация
- Пользователь должен принадлежать текущему tenant (магазину)
- Данные изолированы по tenant схемам

---

## 🔧 Возможные улучшения

### В будущем можно добавить:

1. **Модель Supplier** - заменить `supplier_name` строку на FK
2. **Автоматическое списание** - автоматически уменьшать количество в партиях при продаже
3. **История движения партий** - логирование всех изменений количества
4. **Резервирование товаров** - блокировка партий под заказы
5. **Серийные номера** - для товаров с индивидуальным учетом
6. **Акции и скидки** - связь партий с промо-акциями
7. **Мульти-складской учет** - партии на разных складах
8. **Интеграция с закупками** - автоматическое создание партий из накладных

---

## 📞 Контакты и поддержка

При возникновении вопросов или проблем:
1. Проверьте этот README
2. Изучите код моделей и serializers
3. Проверьте примеры API запросов

---

**Дата последнего обновления:** 2024-11-16
**Версия модуля:** 2.0.0
**Django Version:** 5.1.3
**DRF Version:** 3.15.2
