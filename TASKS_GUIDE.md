# 📋 Руководство по Tasks App (Задачи персонала)

## 🎯 Обзор

Приложение **Tasks** - полноценная система управления задачами персонала с фото-отчётами.

### Ключевые возможности:
- ✅ **Создание и назначение задач** сотрудникам
- ✅ **Приоритеты и статусы** (6 статусов, 4 приоритета)
- ✅ **Сроки выполнения** с автоматической проверкой просрочки
- ✅ **Фото-отчёты** о выполнении с множественной загрузкой
- ✅ **Комментарии** к задачам
- ✅ **Шаблоны задач** для быстрого создания
- ✅ **Уведомления** через Django Signals
- ✅ **Контроль доступа** (сотрудники видят только свои задачи)

---

## 📊 Модели

### 1. Task (Задача)

Основная модель задачи с полным жизненным циклом.

**Поля:**
- `title` - название задачи
- `description` - описание
- `assigned_to` - кому назначена (FK к Employee)
- `created_by` - кто создал (FK к Employee)
- `priority` - приоритет (low, medium, high, urgent)
- `status` - статус (pending, in_progress, on_review, completed, rejected, cancelled)
- `due_date` - срок выполнения
- `estimated_hours` - ожидаемое время (часов)
- `category` - категория
- `started_at`, `completed_at` - даты начала и завершения
- `rejection_reason` - причина отклонения

**Статусы:**
1. **pending** - Ожидает (создана, но не начата)
2. **in_progress** - В работе
3. **on_review** - На проверке (отчёт отправлен)
4. **completed** - Выполнена (одобрена)
5. **rejected** - Отклонена (требуется доработка)
6. **cancelled** - Отменена

**Приоритеты:**
- **low** - Низкий
- **medium** - Средний
- **high** - Высокий
- **urgent** - Срочно

**Свойства:**
- `is_overdue` - просрочена ли задача
- `time_left` - оставшееся время

**Методы:**
- `start_task()` - начать выполнение (pending → in_progress)
- `submit_for_review()` - отправить на проверку (in_progress → on_review)
- `approve()` - одобрить (on_review → completed)
- `reject(reason)` - отклонить (on_review → rejected)

---

### 2. TaskComment (Комментарий)

Комментарии к задаче для обсуждения.

**Поля:**
- `task` - FK к Task
- `author` - FK к Employee
- `text` - текст комментария
- `created_at`, `updated_at` - даты

---

### 3. TaskReport (Отчёт о выполнении)

Отчёт сотрудника о выполненной работе.

**Поля:**
- `task` - FK к Task
- `submitted_by` - FK к Employee
- `description` - описание выполненной работы
- `actual_hours` - фактическое время (часов)
- `submitted_at` - дата отправки

**Связь:**
- `photos` - связь с TaskReportPhoto (множественная загрузка фото)

---

### 4. TaskReportPhoto (Фото к отчёту)

Фото-доказательства выполнения задачи.

**Поля:**
- `report` - FK к TaskReport
- `photo` - изображение (ImageField)
- `caption` - подпись к фото
- `uploaded_at` - дата загрузки

**Путь загрузки:** `tasks/reports/{task_id}/{filename}`

---

### 5. TaskTemplate (Шаблон задачи)

Шаблоны для быстрого создания повторяющихся задач.

**Поля:**
- `title`, `description`, `category` - как в Task
- `priority`, `estimated_hours` - по умолчанию
- `created_by` - кто создал
- `is_active` - активен ли шаблон

**Метод:**
- `create_task(assigned_to, due_date)` - создать задачу из шаблона

---

## 🔌 API Endpoints

### Tasks

```bash
# Список всех задач (с фильтрацией доступа)
GET /api/tasks/tasks/

# Создать задачу
POST /api/tasks/tasks/

# Детали задачи
GET /api/tasks/tasks/{id}/

# Обновить задачу
PUT /api/tasks/tasks/{id}/

# Удалить задачу
DELETE /api/tasks/tasks/{id}/
```

### Специальные actions для задач

```bash
# Мои задачи (назначенные мне)
GET /api/tasks/tasks/my_tasks/

# Созданные мной
GET /api/tasks/tasks/created_by_me/

# Просроченные
GET /api/tasks/tasks/overdue/

# Задачи на сегодня
GET /api/tasks/tasks/today/

# Статистика
GET /api/tasks/tasks/stats/

# Начать выполнение
POST /api/tasks/tasks/{id}/start/

# Отправить на проверку
POST /api/tasks/tasks/{id}/submit_for_review/

# Одобрить
POST /api/tasks/tasks/{id}/approve/

# Отклонить
POST /api/tasks/tasks/{id}/reject/
Body: {"reason": "Причина отклонения"}

# Отменить
POST /api/tasks/tasks/{id}/cancel/
```

### Комментарии

```bash
# Список комментариев
GET /api/tasks/comments/

# Добавить комментарий
POST /api/tasks/comments/
Body: {"task": 1, "text": "Текст комментария"}

# Детали комментария
GET /api/tasks/comments/{id}/
```

### Отчёты

```bash
# Список отчётов
GET /api/tasks/reports/

# Создать отчёт с фото
POST /api/tasks/reports/
Content-Type: multipart/form-data
Body:
  - task: 1
  - description: "Описание работы"
  - actual_hours: 2.5
  - photos: [file1.jpg, file2.jpg]  # массив файлов
  - photo_captions: ["Фото 1", "Фото 2"]  # подписи

# Детали отчёта
GET /api/tasks/reports/{id}/
```

### Шаблоны

```bash
# Список шаблонов
GET /api/tasks/templates/

# Создать шаблон
POST /api/tasks/templates/

# Создать задачу из шаблона
POST /api/tasks/templates/{id}/create_task/
Body: {
  "assigned_to": 5,
  "due_date": "2025-11-20T18:00:00Z"
}
```

---

## 💡 Примеры использования

### 1. Создание задачи

```bash
POST /api/tasks/tasks/
Authorization: Bearer <token>
X-Tenant-Key: <key>

{
  "title": "Провести инвентаризацию склада",
  "description": "Проверить остатки товаров на складе №1 и обновить данные в системе",
  "assigned_to": 5,
  "priority": "high",
  "category": "Склад",
  "due_date": "2025-11-18T18:00:00Z",
  "estimated_hours": 4
}
```

**Ответ:**
```json
{
  "id": 15,
  "title": "Провести инвентаризацию склада",
  "assigned_to_name": "Иванов Иван Иванович",
  "created_by_name": "Петров Пётр Петрович",
  "status": "pending",
  "priority": "high",
  "due_date": "2025-11-18T18:00:00Z",
  "is_overdue": false,
  "time_left": "2д 5ч"
}
```

---

### 2. Начать выполнение задачи

```bash
POST /api/tasks/tasks/15/start/
```

**Результат:**
- Статус: `pending` → `in_progress`
- `started_at` = текущее время

---

### 3. Отправка отчёта с фото

```bash
POST /api/tasks/reports/
Content-Type: multipart/form-data

task=15
description=Инвентаризация завершена. Все остатки проверены и обновлены.
actual_hours=3.5
photos=@photo1.jpg
photos=@photo2.jpg
photos=@photo3.jpg
photo_captions=Склад общий вид
photo_captions=Полка А стеллаж 1
photo_captions=Акт инвентаризации
```

**Результат:**
- Создаётся TaskReport
- Загружаются 3 фото
- Задача автоматически переходит в статус `on_review`

**Ответ:**
```json
{
  "id": 8,
  "task": 15,
  "submitted_by_name": "Иванов Иван Иванович",
  "description": "Инвентаризация завершена...",
  "actual_hours": "3.50",
  "photos": [
    {
      "id": 21,
      "photo": "/media/tasks/reports/15/photo1.jpg",
      "photo_url": "http://localhost:8000/media/tasks/reports/15/photo1.jpg",
      "caption": "Склад общий вид",
      "uploaded_at": "2025-11-16T15:30:00Z"
    },
    {
      "id": 22,
      "photo": "/media/tasks/reports/15/photo2.jpg",
      "photo_url": "http://localhost:8000/media/tasks/reports/15/photo2.jpg",
      "caption": "Полка А стеллаж 1",
      "uploaded_at": "2025-11-16T15:30:01Z"
    },
    {
      "id": 23,
      "photo": "/media/tasks/reports/15/photo3.jpg",
      "photo_url": "http://localhost:8000/media/tasks/reports/15/photo3.jpg",
      "caption": "Акт инвентаризации",
      "uploaded_at": "2025-11-16T15:30:02Z"
    }
  ],
  "submitted_at": "2025-11-16T15:30:00Z"
}
```

---

### 4. Одобрение задачи

```bash
POST /api/tasks/tasks/15/approve/
```

**Результат:**
- Статус: `on_review` → `completed`
- `completed_at` = текущее время

---

### 5. Отклонение задачи

```bash
POST /api/tasks/tasks/15/reject/

{
  "reason": "Не все товары проверены. В акте отсутствует секция Б."
}
```

**Результат:**
- Статус: `on_review` → `rejected`
- `rejection_reason` = указанная причина

---

### 6. Создание задачи из шаблона

Сначала создайте шаблон:

```bash
POST /api/tasks/templates/

{
  "title": "Ежедневная уборка торгового зала",
  "description": "1. Протереть полки\n2. Помыть пол\n3. Вынести мусор",
  "category": "Уборка",
  "priority": "medium",
  "estimated_hours": 1.5
}
```

Затем создайте задачу:

```bash
POST /api/tasks/templates/3/create_task/

{
  "assigned_to": 7,
  "due_date": "2025-11-17T09:00:00Z"
}
```

---

### 7. Получение статистики

```bash
GET /api/tasks/tasks/stats/
```

**Ответ:**
```json
{
  "total": 45,
  "by_status": {
    "pending": 12,
    "in_progress": 18,
    "on_review": 5,
    "completed": 8,
    "rejected": 1,
    "cancelled": 1
  },
  "by_priority": {
    "low": 10,
    "medium": 25,
    "high": 8,
    "urgent": 2
  },
  "overdue": 3
}
```

---

### 8. Мои задачи на сегодня

```bash
GET /api/tasks/tasks/my_tasks/?status=pending,in_progress
GET /api/tasks/tasks/today/
```

---

## 🔔 Уведомления (Signals)

### Автоматические уведомления:

1. **При создании задачи:**
   - Уведомление назначенному сотруднику

2. **При отправке на проверку:**
   - Уведомление создателю задачи

3. **При одобрении:**
   - Уведомление исполнителю

4. **При отклонении:**
   - Уведомление исполнителю с причиной

5. **При добавлении комментария:**
   - Уведомления исполнителю и создателю

6. **При отправке отчёта:**
   - Уведомление создателю задачи

---

## 🔐 Контроль доступа

### Правила доступа:

1. **Сотрудники (cashier, stockkeeper):**
   - Видят только задачи, назначенные им или созданные ими
   - Могут начинать, выполнять и отправлять на проверку свои задачи
   - Не могут одобрять/отклонять задачи

2. **Менеджеры и владельцы:**
   - Видят все задачи
   - Могут создавать, назначать, одобрять и отклонять задачи
   - Могут отменять задачи

### Операции по ролям:

| Действие | Сотрудник | Менеджер/Владелец |
|----------|-----------|-------------------|
| Создать задачу | ✅ | ✅ |
| Начать выполнение | ✅ (свою) | ✅ |
| Отправить на проверку | ✅ (свою) | ✅ |
| Одобрить | ❌ | ✅ |
| Отклонить | ❌ | ✅ |
| Отменить | ❌ | ✅ |
| Комментировать | ✅ | ✅ |
| Отчёт с фото | ✅ (свою) | ✅ |

---

## 📱 Загрузка фото

### Поддерживаемые форматы:
- JPEG (.jpg, .jpeg)
- PNG (.png)
- GIF (.gif)
- BMP (.bmp)

### Ограничения:
- Размер файла: зависит от настроек Django (по умолчанию 2.5MB)
- Количество фото: неограниченно

### Путь хранения:
```
media/
└── tasks/
    └── reports/
        └── {task_id}/
            ├── photo1.jpg
            ├── photo2.jpg
            └── photo3.jpg
```

---

## 🚀 Быстрый старт

```bash
# 1. Миграции
python manage.py makemigrations tasks
python manage.py migrate

# 2. Создать шаблоны задач (опционально)
# Через админку или API

# 3. Создать задачу
curl -X POST http://localhost:8000/api/tasks/tasks/ \
  -H "Authorization: Bearer <token>" \
  -H "X-Tenant-Key: <key>" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Провести уборку",
    "description": "Помыть полы в торговом зале",
    "assigned_to": 5,
    "priority": "medium",
    "category": "Уборка",
    "due_date": "2025-11-17T18:00:00Z"
  }'

# 4. Сотрудник начинает работу
curl -X POST http://localhost:8000/api/tasks/tasks/1/start/

# 5. Отправка отчёта с фото
curl -X POST http://localhost:8000/api/tasks/reports/ \
  -H "Authorization: Bearer <token>" \
  -F "task=1" \
  -F "description=Уборка выполнена" \
  -F "actual_hours=1.5" \
  -F "photos=@photo1.jpg" \
  -F "photos=@photo2.jpg"

# 6. Менеджер одобряет
curl -X POST http://localhost:8000/api/tasks/tasks/1/approve/
```

---

## 📊 Интеграция с другими модулями

### С Users (Employee):
- Задачи привязаны к сотрудникам
- Контроль доступа по ролям

### С Analytics (будущее):
- Статистика по выполненным задачам
- Производительность сотрудников
- Среднее время выполнения

---

## ✅ Готово к использованию!

Tasks app полностью готов для управления задачами персонала с фото-отчётами!

**Основные возможности:**
- ✅ Полный жизненный цикл задачи (6 статусов)
- ✅ Фото-отчёты с множественной загрузкой
- ✅ Комментарии и обсуждения
- ✅ Шаблоны для быстрого создания
- ✅ Автоматические уведомления
- ✅ Контроль доступа по ролям
- ✅ API для мобильных приложений

Просто запусти миграции и начинай использовать! 🚀
