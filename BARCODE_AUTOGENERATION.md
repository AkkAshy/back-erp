# Автоматическая генерация штрихкодов для товаров

## Описание

При создании товара, если **баркод не указан**, система автоматически генерирует EAN-13 совместимый штрихкод.

## Как это работает

### 1. Автоматическая генерация при создании

Когда вы создаете товар **без** указания `barcode`:

```json
POST /api/products/products/
{
  "name": "Молоко 3.2%",
  "sku": "MLK-001",
  "category": 1,
  "unit": 1
  // НЕ указываем barcode!
}
```

Система автоматически сгенерирует штрихкод в формате EAN-13 (13 цифр):
```
4870123456782
```

### 2. Формат генерируемого штрихкода

**Формат:** `487` + `9 случайных цифр` + `контрольная сумма`

Например:
- `4870123456782`
- `4879876543215`
- `4874562389017`

**Где:**
- `487` — префикс (можно изменить в коде)
- `012345678` — 9 случайных цифр
- `2` — контрольная сумма EAN-13

### 3. Контрольная сумма EAN-13

Система автоматически вычисляет правильную контрольную сумму по алгоритму EAN-13:

1. Суммируются цифры на нечетных позициях (1, 3, 5, 7, 9, 11)
2. Суммируются цифры на четных позициях (2, 4, 6, 8, 10, 12) и умножаются на 3
3. Складываются два результата
4. Контрольная цифра = (10 - (сумма % 10)) % 10

Это значит что сгенерированный баркод **можно сканировать реальным сканером штрихкодов**!

---

## Примеры использования

### ✅ Вариант 1: Создать товар без баркода (автогенерация)

```bash
POST /api/products/products/
Headers:
  Authorization: Bearer {token}
  X-Tenant-Key: {tenant_key}

Body:
{
  "name": "Хлеб белый",
  "sku": "BRD-001",
  "category": 1,
  "unit": 1,
  "description": "Свежий белый хлеб"
}
```

**Ответ:**
```json
{
  "status": "success",
  "data": {
    "id": 123,
    "name": "Хлеб белый",
    "sku": "BRD-001",
    "barcode": "4876543210987",  ← Автоматически сгенерирован!
    "category": 1,
    "unit": 1
  }
}
```

---

### ✅ Вариант 2: Создать товар со своим баркодом

Если вы хотите использовать **свой баркод**, просто укажите его:

```bash
POST /api/products/products/
Body:
{
  "name": "Coca-Cola 0.5л",
  "sku": "CCL-500",
  "barcode": "5449000000996",  ← Свой баркод
  "category": 2,
  "unit": 1
}
```

**Ответ:**
```json
{
  "id": 124,
  "name": "Coca-Cola 0.5л",
  "sku": "CCL-500",
  "barcode": "5449000000996",  ← Используется ваш баркод
  "category": 2,
  "unit": 1
}
```

---

## Frontend интеграция

### React/TypeScript

```typescript
// src/services/products.ts
import api from '@/utils/api';

/**
 * Создать товар с автогенерацией баркода
 */
export const createProduct = async (productData: {
  name: string;
  sku: string;
  category: number;
  unit: number;
  description?: string;
  barcode?: string; // Опционально!
}) => {
  const response = await api.post('/products/products/', productData);
  return response.data;
};
```

### Пример компонента

```typescript
// components/ProductForm.tsx
import { useState } from 'react';
import { createProduct } from '@/services/products';

export const ProductForm = () => {
  const [formData, setFormData] = useState({
    name: '',
    sku: '',
    category: '',
    unit: '',
    description: '',
    barcode: '', // Оставьте пустым для автогенерации!
  });

  const [generatedBarcode, setGeneratedBarcode] = useState('');

  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();

    try {
      // Если barcode пустой - НЕ отправляем его (система сгенерирует)
      const data: any = {
        name: formData.name,
        sku: formData.sku,
        category: parseInt(formData.category),
        unit: parseInt(formData.unit),
      };

      // Добавляем barcode только если пользователь его указал
      if (formData.barcode) {
        data.barcode = formData.barcode;
      }

      const result = await createProduct(data);

      // Сохраняем сгенерированный баркод
      setGeneratedBarcode(result.data.barcode);

      alert(`✅ Товар создан! Баркод: ${result.data.barcode}`);
    } catch (error) {
      console.error('Ошибка:', error);
    }
  };

  return (
    <form onSubmit={handleSubmit}>
      <div>
        <label>Название:</label>
        <input
          type="text"
          value={formData.name}
          onChange={(e) => setFormData({ ...formData, name: e.target.value })}
          required
        />
      </div>

      <div>
        <label>Артикул (SKU):</label>
        <input
          type="text"
          value={formData.sku}
          onChange={(e) => setFormData({ ...formData, sku: e.target.value })}
          required
        />
      </div>

      <div>
        <label>
          Штрихкод (оставьте пустым для автогенерации):
        </label>
        <input
          type="text"
          value={formData.barcode}
          onChange={(e) => setFormData({ ...formData, barcode: e.target.value })}
          placeholder="Оставьте пустым для автогенерации"
        />
        <small>Если не указан, будет сгенерирован автоматически</small>
      </div>

      {/* Другие поля */}

      <button type="submit">Создать товар</button>

      {generatedBarcode && (
        <div style={{ marginTop: '20px', padding: '10px', background: '#d4edda' }}>
          <strong>Сгенерированный баркод:</strong> {generatedBarcode}
        </div>
      )}
    </form>
  );
};
```

---

## Проверка баркода

### 1. Через API

Получить товар и посмотреть его баркод:

```bash
GET /api/products/products/123/
```

Ответ:
```json
{
  "id": 123,
  "name": "Молоко",
  "barcode": "4876543210987",
  ...
}
```

### 2. Через print-label endpoint

```bash
GET /api/products/products/123/print-label/
```

Вернет штрихкод в виде изображения base64 для печати этикетки.

---

## Изменение префикса баркода

Если вы хотите изменить префикс `487` на свой:

1. Откройте `products/models.py`
2. Найдите метод `save()` в классе `Product`
3. Измените строку:
   ```python
   prefix = "487"  # Ваш новый префикс (3 цифры)
   ```

Например:
```python
prefix = "999"  # Баркоды будут начинаться с 999
```

Перезапустите сервер после изменения.

---

## Уникальность баркодов

**Важно:** Система НЕ проверяет уникальность автогенерируемых баркодов!

Вероятность коллизии очень низкая (1 из 1 миллиарда), но если нужна 100% уникальность:

### Решение 1: Использовать UUID в баркоде

Измените метод `save()`:
```python
def save(self, *args, **kwargs):
    if not self.barcode:
        import uuid
        unique_id = str(uuid.uuid4().int)[:12]  # 12 цифр
        self.barcode = unique_id
    super().save(*args, **kwargs)
```

### Решение 2: Проверять на уникальность

```python
def save(self, *args, **kwargs):
    if not self.barcode:
        import random
        while True:
            # Генерируем баркод
            prefix = "487"
            random_part = ''.join([str(random.randint(0, 9)) for _ in range(9)])
            barcode_candidate = prefix + random_part

            # Вычисляем контрольную сумму
            odd_sum = sum(int(barcode_candidate[i]) for i in range(0, 12, 2))
            even_sum = sum(int(barcode_candidate[i]) for i in range(1, 12, 2))
            total = odd_sum + (even_sum * 3)
            check_digit = (10 - (total % 10)) % 10

            barcode_final = barcode_candidate + str(check_digit)

            # Проверяем что такого баркода еще нет
            if not Product.objects.filter(barcode=barcode_final).exists():
                self.barcode = barcode_final
                break

    super().save(*args, **kwargs)
```

---

## Часто задаваемые вопросы

### 1. Можно ли использовать свой баркод вместо автогенерации?

**Да!** Просто укажите `barcode` при создании товара:
```json
{
  "name": "Товар",
  "sku": "ABC-123",
  "barcode": "1234567890123"  ← Ваш баркод
}
```

### 2. Можно ли изменить баркод после создания?

**Да**, через PATCH запрос:
```bash
PATCH /api/products/products/123/
Body: { "barcode": "9999999999999" }
```

### 3. Генерируется ли баркод для существующих товаров?

**Нет**, автогенерация работает только при создании (если `barcode` пуст).

Чтобы сгенерировать баркоды для существующих товаров без баркода:

```python
# В Django shell
from products.models import Product

products_without_barcode = Product.objects.filter(barcode='')
for product in products_without_barcode:
    product.save()  # Метод save() сгенерирует баркод
```

### 4. Работает ли это с реальными сканерами штрихкодов?

**Да!** Сгенерированные баркоды совместимы с EAN-13 и имеют правильную контрольную сумму.

Однако префикс `487` не зарегистрирован в GS1 (международная система баркодов), поэтому:
- ✅ Работает для внутреннего использования в магазине
- ❌ НЕ подходит для продажи товаров через крупные сети/маркетплейсы

Для маркетплейсов используйте официальные баркоды GS1.

---

## Резюме

### Автогенерация включена ✅

- При создании товара **без** `barcode` → система генерирует EAN-13 баркод автоматически
- Формат: `487XXXXXXXXX` + контрольная сумма (13 цифр)
- Совместим с реальными сканерами штрихкодов
- Можно указать свой баркод — автогенерация не сработает

### Пример:

```bash
# Создать товар БЕЗ указания barcode
POST /api/products/products/
{
  "name": "Новый товар",
  "sku": "NEW-001",
  ...
}

# Ответ:
{
  "barcode": "4876543210987"  ← Сгенерирован автоматически!
}
```

Готово! 🔢
