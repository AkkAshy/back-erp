# 🔧 Исправление критической ошибки учета количества товара

**Дата:** 2025-11-22
**Приоритет:** КРИТИЧЕСКИЙ

---

## ❌ Проблема

При продаже товара **не уменьшалось количество** на складе. Можно было сканировать и продавать товар бесконечно, даже если на складе было всего 10 штук.

### Обнаруженные баги:

1. **Нет проверки наличия при сканировании** - можно добавить в продажу больше, чем есть на складе
2. **Нет проверки наличия при добавлении товара** - аналогично
3. **Количество не уменьшается** - после завершения продажи товар остается на складе

---

## ✅ Решение

### 1. Добавлена проверка наличия при сканировании

**Файл:** [sales/views.py](sales/views.py#L489-L512)

```python
# ПРОВЕРКА НАЛИЧИЯ ТОВАРА
available_qty = product.available_quantity

# Проверяем, сколько уже добавлено в текущую продажу
existing_item = SaleItem.objects.filter(
    sale=sale,
    product=product,
    batch=batch
).first()

current_qty_in_sale = existing_item.quantity if existing_item else Decimal('0')
total_requested = current_qty_in_sale + quantity

if total_requested > available_qty:
    return Response({
        'status': 'error',
        'code': 'insufficient_stock',
        'message': f'Недостаточно товара на складе. Доступно: {available_qty}, запрошено: {total_requested}',
        'data': {
            'available': str(available_qty),
            'requested': str(total_requested),
            'current_in_sale': str(current_qty_in_sale)
        }
    }, status=status.HTTP_400_BAD_REQUEST)
```

**Что делает:**
- Получает доступное количество товара через `product.available_quantity`
- Проверяет сколько уже добавлено в текущую продажу
- Блокирует добавление если запрошенное количество больше доступного
- Возвращает понятную ошибку с информацией о доступном количестве

### 2. Добавлена проверка наличия при add_item

**Файл:** [sales/views.py](sales/views.py#L593-L614)

Аналогичная проверка добавлена в метод `add_item()`.

### 3. Уменьшение количества товара при завершении резервирования

**Файл:** [products/models.py](products/models.py#L1310-L1336)

```python
def complete(self):
    """
    Завершает резервирование (товар продан).
    ВАЖНО: Уменьшает количество товара в партии или общий запас!
    """
    if self.status == 'active':
        # Уменьшаем количество товара
        if self.batch:
            # Списываем из конкретной партии
            self.batch.quantity -= self.quantity
            if self.batch.quantity < 0:
                self.batch.quantity = 0
            self.batch.save(update_fields=['quantity', 'updated_at'])
        else:
            # Списываем из общего запаса товара (если нет партии)
            if hasattr(self.product, 'pricing') and self.product.pricing:
                current_stock = self.product.pricing.stock_quantity or 0
                new_stock = current_stock - self.quantity
                if new_stock < 0:
                    new_stock = 0
                self.product.pricing.stock_quantity = new_stock
                self.product.pricing.save(update_fields=['stock_quantity', 'updated_at'])

        # Меняем статус резервирования
        self.status = 'completed'
        self.save()
```

**Что делает:**
- При завершении резервирования уменьшает количество в партии
- Если партия не указана - уменьшает общий запас товара в `Pricing.stock_quantity`
- Защита от отрицательных значений (минимум 0)
- Обновляет `updated_at` для отслеживания изменений

---

## 🎯 Как это работает теперь

### Сценарий: Продажа товара

```
1. Кассир сканирует товар
   ↓
2. Проверяется available_quantity товара
   ↓
3. Если товара достаточно → добавляется в продажу (pending)
   Если НЕ достаточно → Ошибка "Недостаточно товара на складе"
   ↓
4. Кассир завершает продажу (complete)
   ↓
5. Создается резервирование (StockReservation)
   ↓
6. Резервирование завершается (reservation.complete())
   ↓
7. Количество товара УМЕНЬШАЕТСЯ:
   - В партии: batch.quantity -= quantity
   - В общем запасе: pricing.stock_quantity -= quantity
```

### Пример сценария:

**Исходные данные:**
- Товар: "Молоко 1л"
- На складе: 10 штук
- Цена: 70 сом

**Действия:**

```bash
# 1. Сканируем 5 штук
POST /api/sales/sales/scan_item/
{
  "session": 1,
  "product": 123,
  "quantity": 5
}
→ ✅ Success (доступно 10, запрошено 5)

# 2. Пытаемся сканировать еще 7 штук
POST /api/sales/sales/scan_item/
{
  "session": 1,
  "product": 123,
  "quantity": 7
}
→ ❌ Error: "Недостаточно товара на складе. Доступно: 10, запрошено: 12"
  (уже 5 в продаже + 7 новых = 12 > 10)

# 3. Сканируем еще 3 штуки (всего 8)
POST /api/sales/sales/scan_item/
{
  "session": 1,
  "product": 123,
  "quantity": 3
}
→ ✅ Success (доступно 10, запрошено 8)

# 4. Завершаем продажу
POST /api/sales/sales/{id}/complete/
→ ✅ Success
  - Создано резервирование на 8 штук
  - Товар списан: stock_quantity: 10 → 2
```

**Результат:**
- ✅ На складе осталось: **2 штуки**
- ✅ Продано: **8 штук**
- ✅ Невозможно продать больше, чем есть

---

## 🧪 Тестирование

### Тест 1: Попытка продать больше чем есть

```bash
# Товар на складе: 10 штук

# Пытаемся сканировать 15 штук
curl -X POST http://localhost:8000/api/sales/sales/scan_item/ \
  -H "Authorization: Bearer $TOKEN" \
  -H "X-Tenant-Key: $TENANT_KEY" \
  -d '{
    "session": 1,
    "product": 123,
    "quantity": 15
  }'

# Ожидаемый результат:
{
  "status": "error",
  "code": "insufficient_stock",
  "message": "Недостаточно товара на складе. Доступно: 10, запрошено: 15",
  "data": {
    "available": "10",
    "requested": "15",
    "current_in_sale": "0"
  }
}
```

### Тест 2: Уменьшение количества после продажи

```bash
# Исходное количество: 100 штук

# 1. Сканируем 30 штук
POST /api/sales/sales/scan_item/
{"session": 1, "product": 123, "quantity": 30}
→ Success

# 2. Завершаем продажу
POST /api/sales/sales/{id}/complete/
→ Success

# 3. Проверяем количество товара
GET /api/products/products/123/

# Ожидаемый результат:
{
  "id": 123,
  "available_quantity": "70.000",  # Было 100, продали 30, осталось 70
  "pricing": {
    "stock_quantity": "70.000"
  }
}
```

### Тест 3: Попытка сканировать после добавления в продажу

```bash
# Товар на складе: 10 штук

# 1. Добавляем 6 штук в продажу
POST /api/sales/sales/scan_item/
{"session": 1, "product": 123, "quantity": 6}
→ Success

# 2. Пытаемся добавить еще 6 штук (всего будет 12 > 10)
POST /api/sales/sales/scan_item/
{"session": 1, "product": 123, "quantity": 6}

# Ожидаемый результат:
{
  "status": "error",
  "code": "insufficient_stock",
  "message": "Недостаточно товара на складе. Доступно: 10, запрошено: 12",
  "data": {
    "available": "10",
    "requested": "12",
    "current_in_sale": "6"  # Показывает что уже 6 в продаже
  }
}
```

---

## 📊 Исправленные файлы

1. **sales/views.py**
   - `SaleViewSet.scan_item()` - добавлена проверка наличия
   - `SaleViewSet.add_item()` - добавлена проверка наличия

2. **products/models.py**
   - `StockReservation.complete()` - добавлено уменьшение количества товара

---

## ⚠️ Важные моменты

1. **Проверка available_quantity**
   - Учитывает уже зарезервированный товар
   - Учитывает товар в текущей незавершенной продаже
   - Возвращает понятную ошибку с деталями

2. **Уменьшение при complete()**
   - Работает с партиями (batch.quantity)
   - Работает с общим запасом (pricing.stock_quantity)
   - Защита от отрицательных значений

3. **Атомарность операций**
   - Проверка и добавление должны быть в одной транзакции
   - Уменьшение количества происходит только при успешном завершении резервирования

---

## 🎉 Результат

✅ **Невозможно продать больше, чем есть на складе**
✅ **Количество товара корректно уменьшается после продажи**
✅ **Понятные сообщения об ошибках**
✅ **Защита от отрицательных значений**

---

**Статус:** ✅ ИСПРАВЛЕНО
**Дата:** 2025-11-22
